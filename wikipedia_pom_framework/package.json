{
  "name": "wikipedia-pom-framework",
  "version": "1.0.0",
  "description": "Wikipedia Android app scenario automated with a Page Object Model framework (WebdriverIO + Appium), reusing the POM, utilities and configuration patterns from Days 6-8.",
  "type": "module",
  "scripts": {
    "test": "wdio run ./wdio.conf.js",
    "allure:generate": "allure generate allure-results --clean --single-file -o allure-report",
    "allure:open": "allure open allure-report",
    "allure:report": "npm run allure:generate && npm run allure:open"
  },
  "devDependencies": {
    "@wdio/allure-reporter": "^9.27.1",
    "@wdio/cli": "^9.27.1",
    "@wdio/local-runner": "^9.27.1",
    "@wdio/mocha-framework": "^9.27.1",
    "@wdio/spec-reporter": "^9.27.1",
    "allure-commandline": "^2.41.0"
  }
}
